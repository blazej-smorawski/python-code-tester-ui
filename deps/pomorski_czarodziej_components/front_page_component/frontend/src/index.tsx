import React from "react"
import ReactDOM from "react-dom"
import FrontPageComponent from "./FronPageComponent"

ReactDOM.render(
  <React.StrictMode>
      <FrontPageComponent/>
  </React.StrictMode>,
  document.getElementById("root")
)
